This is a simple console-based Java application that manages bus passes for passengers.

The system allows passengers to:

Apply for a new bus pass

Renew an expired pass

Cancel a pass

View all issued passes

Each passenger can have only one active pass at a time.
